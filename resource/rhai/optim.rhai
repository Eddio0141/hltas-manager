let should_pass_all_frames = false;

fn is_valid(curr) {
    true
}

fn is_better(curr, best) {
    true
}

fn to_string(curr) {
    // you can reference to variables in is_better() here
    "".to_string()
}